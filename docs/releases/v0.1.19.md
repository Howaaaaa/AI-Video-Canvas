## 新增

- AI 图片节点（ImageEditNode）支持提示词模版功能，按钮位于生成按钮旁边，选择模版后内容追加到提示词文本框
- AI 对话节点（AiChatNode）与 AI 图片节点提示词模版菜单样式统一：宽度 w-32、去除自定义滚动条、仅显示模版标题

## 优化

- AI 图片节点默认尺寸调整为 500×200，AI 对话节点调整为 500×300
- 节点注册表（nodeRegistry）新增 defaultWidth/defaultHeight 字段，工厂创建节点时显式赋值 width/height/style，确保默认尺寸可靠生效
